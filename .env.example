# Client bundle (Vite — must be VITE_-prefixed to exist in the browser)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Netlify Functions only (never in the client bundle)
SUPABASE_SERVICE_ROLE_KEY=
E2E_REPORT_SECRET=
